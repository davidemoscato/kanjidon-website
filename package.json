{
  "name": "kanjidon-website",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "cf:build": "node ./cloudflare/stage-assets.mjs",
    "cf:check": "pnpm cf:build && wrangler deploy --dry-run",
    "cf:dev": "pnpm cf:build && wrangler dev",
    "cf:deploy": "pnpm cf:build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.123.0"
  }
}
